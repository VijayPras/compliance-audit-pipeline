# Minimal, always-loaded config. Everything environment-specific lives in
# <profile>_auditpipeline.properties instead, loaded via @PropertySource on
# OrderServiceApplication -- this file's only job is picking the profile.
spring.profiles.active=local
