# Minimal, always-loaded config -- same pattern as order-service. Everything
# environment-specific lives in <profile>_auditpipeline.properties instead,
# loaded via @PropertySource on AuditServiceApplication.
spring.profiles.active=local
